Django
Python
Postgress

## Packages
pip install django
pip install autopep8
pip install psycopg2
pip install psycopg2-binary

## run
python manage.py is used to run django
e.g. python manage.py runserver

postgress - use the pgAdmin app to manage the servers
update the database json in settings.py,
python manage.py migrate (for db)
